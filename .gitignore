# This repository holds the web app only.
#
# The desktop builds (Windows .exe / macOS .app) live outside version control
# for now. These entries keep them from being committed by accident.
save_reminder.py
make_icon.py
build_exe.bat
build_app.sh
Save Now.bat
.github/
app_icon.ico
app_icon.icns
app_icon.png
build/
dist/
*.spec
*.zip

# Note: web/ IS committed on purpose. It is generated by make_web.py, but
# committing it means Vercel serves the site with no build step at all.

__pycache__/
*.pyc
.vercel/
.DS_Store
Thumbs.db
